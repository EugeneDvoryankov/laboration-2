import Model.Model;
import Model.VehicleAttribute;

public class VehicleController {
/*
    private Model model;
    private VehicleAttribute hasTurboModel;

    public VehicleController() {

    }
    /*
    void gas(int amount) {
        double gas = ((double) amount) / 100;
        for (Vehicle.Vehicle vehicle : model.world.vehicles
        ) {
            vehicle.gas(gas);
        }
    }

    void brake(int amount) {
        double gas = ((double) amount) / 100;
        for (Vehicle.Vehicle vehicle : model.world.vehicles
        ) {
            vehicle.brake(gas);
        }
    }

    public void turboOn() {
        hasTurboModel.turboOn();
        for(VehicleAttribute hasTurbo: hasTurboList) {
            hasTurbo.setTurboOn();
        }
    }

    public void turboOff() {
        for(VehicleAttribute hasTurbo: model.HasTurboModel.hasTurboList) {
            hasTurbo.setTurboOff();
        }
    }

    public void liftBed() {
        for(VehicleAttribute hasPlatform: model.HasPlatformModel.hasPlatformList) {
            hasPlatform.raisePlatformAngle(60);
        }
    }

    public void lowerBed() {
        for(VehicleAttribute hasPlatform: model.HasPlatformModel.hasPlatformList) {
            hasPlatform.lowerPlatformAngle(60);
        }
    }

    public void startAllVehicles() {
        for(Vehicle.Vehicle vehicle : model.world.vehicles) {
            vehicle.startEngine();
        }
    }

    public void stopAllVehicles() {
        for(Vehicle.Vehicle vehicle : model.world.vehicles) {
            vehicle.stopEngine();
        }
    }

*/
}
